size(800, 600);
//background(255);
// int offset = 0;
//or (int y = 0; y <= height + 100; y += 60) {
//for (int x = 40; x <= width; x += 80) {

// noFill();
// strokeWeight(1.5);
// stroke(255, 0, 0);
// ellipse(x - offset, y, 100, 100); }
//  if (offset == 0) offset = 40;
//  else offset = 0;
// }
